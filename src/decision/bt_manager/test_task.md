Copilot Task: Sentry BT Test Node Implementation (event_status_pub_test.cpp)1. 任务目标 (Task Objective)请修改并重写当前的 src/test/event_status_pub_test.cpp 文件。该节点是一个针对 RoboMaster 哨兵行为树（BehaviorTree）的自动化测试模拟器。你需要实现一个基于 rclcpp::TimerBase 的状态机，通过定时发布虚拟的 ROS 话题和 TF 变换，来验证行为树的所有逻辑分支。2. 核心约束 (Core Constraints)状态变更打印 (State-Change Logging Only)：不要在 Timer 回调中高频打印日志。必须在类中维护状态缓存（如记录当前的 Phase 或上一次的重要状态），只有当状态或 Phase 发生切换时，才使用带颜色高亮的 RCLCPP_INFO 打印预期行为。TF 驱动空间位置：行为树判断“是否在特定区域（如过洞区、下台阶区）”是通过 map 到 base_link 的 TF 实现的。你需要通过 tf2_ros::TransformBroadcaster 动态平移虚拟坐标来触发这些条件，而非仅仅改变文本。无敌方基地血量判断：之前的“敌方基地血量”判定逻辑已废除，不要在测试代码中引入此变量。3. 状态机剧本 (Phase Scenarios)请在 event_status_pub_test.cpp 中实现一个 phase_ 变量，每隔一定时间（如 8~10 秒）推进到下一个 Phase。必须涵盖以下所有测试：[Phase 1: 生存最高优与回血回弹逻辑]模拟输入：self_health = 20 (低于30%), bullets_remaining = 50 (低于100)。测试目的：验证生存优先级最高。预期 NavTree 强制切入回血回弹目标（HOME）；预期 StanceTree 强制切入 MOVE 姿态。退出测试：3秒后将数据恢复为 self_health = 100, bullets_remaining = 300，预期自动退出回血模式，恢复常规逻辑。[Phase 2: 过隧道与下台阶判定 (依赖 TF)]模拟输入 A (下台阶与队友避让)：将 TF 坐标平移至 StairsZone。发布雷达假数据：检测到台阶下方有队友。测试目的：预期 NavTree 下台阶动作中止（避让队友）。模拟输入 B (解除避让)：清除下方队友雷达信号。预期恢复下台阶 cmd_vel。模拟输入 C (过隧道)：将 TF 坐标平移至 TunnelZone。测试目的：预期触发云台升降逻辑 (lifter_pos 变化)，预期正常通过隧道区。[Phase 3: 宏观战术切换与特殊响应 (前哨站、堡垒)]模拟输入 A (NORMAL)：敌方前哨站未摧毁。预期战术为 NORMAL，行为树优先响应，导航至己方前哨站，StanceTree 触发 ATTACK 姿态。模拟输入 B (DEFEND)：己方基地 HP < 1000，己方堡垒无人占领。预期战术切换为 DEFEND，优先导航去占领己方堡垒，StanceTree 触发 DEFEND（小陀螺）。模拟输入 C (ATTACK)：敌方前哨站被摧毁，己方能量机关激活。预期战术切换为 ATTACK，导航至敌方堡垒进行压制。[Phase 4: 追踪逻辑与巡检逻辑 (战术隔离)]模拟输入 A (强锁敌追踪)：target_valid = true，提供一个视野内的假敌方坐标。测试目的：无论什么战术，GimbalTree 切入强锁敌，NavTree 进行坐标转换并发布追击目标（测试目标防抖逻辑是否生效）。模拟输入 B (战术巡检切换)：target_valid = false（丢失目标）。测试目的：GimbalTree 根据当前的 Tactical Mode（DEFEND/ATTACK/NORMAL）以及当前所在的 TF 语义区域，输出对应的巡检范围 (yaw/pitch)。NavTree 输出对应战术的巡逻点列表。[Phase 5: 姿态树硬件构型法则 (CD 与 超时惩罚)]模拟输入 A (5秒 CD 测试)：在 1Hz 的频率下疯狂交替发送 target_valid = true/false。测试目的：行为树不应跟着 1Hz 疯狂切换姿态，必须验证 5秒 CD 拦截生效。模拟输入 B (3分钟超时刷新)：在代码中伪造一个极长的时间流逝（Time jump）。测试目的：当同一姿态（如 MOVE）保持超过 180 秒时，预期 BT 会触发一次瞬时的异构姿态切换来刷新时间，然后切回原姿态。[Phase 6: 优先级综合校验与 TF 丢失边界测试]模拟输入 A (优先级抢占)：同时触发 [需要回血] + [前哨站受击] + [视野看到敌人]。测试目的：严格验证抢占顺序：回血 > 特殊响应 > 追踪 > 巡逻。表现必须是立刻回血。模拟输入 B (TF 丢失)：停止 tf2_ros::TransformBroadcaster 的发布。测试目的：验证系统不会引发 Segmentation Fault，预期 getCurrentPose() 返回旧缓存或报错，行为树安全挂起。4. 输出要求 (Output Formatting)请输出完整的 event_status_pub_test.cpp 源代码。代码必须使用 C++17 编写，结构清晰，利用 switch (phase_) 管理测试剧本，且必须包含详尽的中文注释解释每一步的测试意图。终端打印请使用 \033[1;32m 等 ANSI 转义码高亮重要阶段。